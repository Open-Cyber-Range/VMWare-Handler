[package]
name = "handler-test-condition"
description = "This is a simple condition package that checks if the current time in epoch seconds is fully divisible"
version = "0.3.1"
authors = ["Rait Rotšan rait.rotsan@cr14.ee"]
license = "MIT"
readme = "readme.md"
assets = [
     ["src/condition.sh", "/usr/bin/test-folder/", "755"],
]

[content]
type = "condition"

[condition]
action = "/usr/bin/test-folder/condition.sh"
interval = 3
